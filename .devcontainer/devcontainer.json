{
  "name": "Java Spring Boot Development",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  
  "forwardPorts": [8083, 3306],
  "portsAttributes": {
    "8083": {
      "label": "Spring Boot App",
      "onAutoForward": "notify"
    },
    "3306": {
      "label": "MySQL Database"
    }
  },

  // MySQL is started automatically via depends_on in docker-compose.yml
  "postCreateCommand": [
    "bash", "-c", 
    "chmod +x .devcontainer/*.sh && echo 'Building project...' && cd ${containerWorkspaceFolder}/app && mvn clean install -DskipTests"
  ],
  "postStartCommand": [
    "bash", "-c",
    "${containerWorkspaceFolder}/.devcontainer/startup.sh"
  ],

  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "pivotal.vscode-boot-dev-pack",
        "ms-vscode.docker",
        "ms-azuretools.vscode-docker",
        "ms-vscode.vscode-json"
      ],
      "settings": {
        "java.home": "/usr/lib/jvm/java-8-openjdk-amd64",
        "java.configuration.updateBuildConfiguration": "automatic",
        "maven.executable.path": "/usr/bin/mvn",
        "java.compile.nullAnalysis.mode": "automatic"
      }
    },
    "codespaces": {
      "openFiles": [
        "README.md",
        "app/src/main/java/com/example/testpath/TestPathApplication.java"
      ]
    }
  },
  
  "remoteUser": "vscode"
}